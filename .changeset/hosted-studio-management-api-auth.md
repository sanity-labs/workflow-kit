---
'@sanity-labs/workflow-kit': patch
---

Authenticate Management API requests with the Studio session Bearer token so project-user lookups work on hosted Studios (`*.sanity.studio`), where cookie-only CORS is blocked.
